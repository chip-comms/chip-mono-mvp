# Python Backend - Local Development Environment
# Copy this file to .env and fill in your values

# Supabase Configuration (shared dev instance)
SUPABASE_URL=http://host.docker.internal:54321
SUPABASE_SECRET_KEY=add_after_setting_up_supabase_db_locally
# AI Provider API Key (get your own free key)
# Get a free Gemini API key: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Hugging Face Token (required for speaker diarization)
# Get a free token: https://huggingface.co/settings/tokens
# Then accept terms at: https://huggingface.co/pyannote/speaker-diarization-3.1
HUGGINGFACE_TOKEN=your_huggingface_token_here

# Server Configuration (for local development)
PORT=8000

# Note: The following are configured automatically:
# - AI_PROVIDER defaults to 'gemini'
# - HOST defaults to '0.0.0.0'
# - CORS is not needed (Edge Functions handle this in production)
