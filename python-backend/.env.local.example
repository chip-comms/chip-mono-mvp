# Python Backend - Local Development Environment
# Copy this file to .env.local and fill in your values

# =============================================================================
# Supabase Configuration (Local Development)
# =============================================================================
# Use local Supabase instance started with ./db-ops.sh start
# Note: Use host.docker.internal to access host machine from Docker container
SUPABASE_URL=http://host.docker.internal:54321
SUPABASE_SECRET_KEY=your_local_service_role_key_here

# Get your local service role key:
# 1. Start Supabase: ./db-ops.sh start
# 2. Open Studio: http://localhost:54323
# 3. Go to Settings → API
# 4. Copy the "service_role" key (NOT the anon key)

# =============================================================================
# AI Provider Configuration
# =============================================================================
# Choose your AI provider: gemini, openai, or anthropic
AI_PROVIDER=gemini

# Gemini API Key (Recommended for development - free tier is generous)
# Get a free API key: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API Key (Alternative)
# Get API key: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (Alternative)
# Get API key: https://console.anthropic.com/
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# =============================================================================
# Hugging Face Configuration (Required for Speaker Diarization)
# =============================================================================
# Get a free token: https://huggingface.co/settings/tokens
# Then accept terms at: https://huggingface.co/pyannote/speaker-diarization-3.1
HUGGINGFACE_TOKEN=your_huggingface_token_here

# =============================================================================
# Server Configuration
# =============================================================================
# Port inside the container (don't change this)
PORT=8080

# API Key for authenticating edge function requests (optional for local dev)
API_KEY=local-dev-key-not-needed

# =============================================================================
# Storage Configuration (Optional - defaults work for local dev)
# =============================================================================
# UPLOAD_DIR=./storage/uploads
# TEMP_DIR=./storage/temp

# =============================================================================
# CORS Configuration (Optional - not needed for local dev)
# =============================================================================
# CORS_ORIGINS=http://localhost:3000
